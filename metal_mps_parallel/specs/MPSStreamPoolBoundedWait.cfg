
\* TLC Configuration for MPSStreamPoolBoundedWait
\* Purpose: Verify bounded wait property

\* Constants - Small values for tractable model checking
CONSTANT
    NumStreams = 4
    NumThreads = 3
    MaxOperations = 15
    MaxWaitBound = 5

\* Specification
SPECIFICATION Spec

\* Invariants to check
INVARIANT
    TypeOK
    SafetyInvariant
    BoundedWaitInvariant

\* Properties to check
PROPERTY
    DeadlockFree

\* Note: BoundedWaitProgress is a liveness property (uses ~>)
\* Uncomment to check liveness (may take longer):
\* PROPERTY BoundedWaitProgress
