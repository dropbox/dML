\* TLC Configuration for MPSEvent
\* Run with small constants for bounded model checking

\* Constants
CONSTANT
    NumEvents = 3       \* Small event pool
    NumStreams = 2      \* 2 streams
    NumThreads = 2      \* 2 concurrent threads
    MaxOperations = 10  \* Bound on operations

\* Specification
SPECIFICATION Spec

\* Check safety invariant
INVARIANT SafetyInvariant

\* Check callback survival (key property)
INVARIANT CallbackSurvival

\* Check event ID uniqueness
INVARIANT EventIDUniqueness

\* Check deadlock freedom (state-level invariant, checked in all reachable states)
INVARIANT DeadlockFree
